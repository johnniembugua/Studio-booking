import 'package:flutter/material.dart';

class StudioLocation extends StatelessWidget {
  const StudioLocation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Studio Location'),
      ),
    );
  }
}
